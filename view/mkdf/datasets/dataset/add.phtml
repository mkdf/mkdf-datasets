<h1>Add dataset</h1>

<?php
$form = $this->form;

$form->get('title')->setAttributes([
    'class'=>'form-control',
    'placeholder'=>'Dataset title'
]);

$form->get('description')->setAttributes([
    'class'=>'form-control',
    'placeholder'=>'Dataset description'
]);

$form->get('datasetTypes')->setAttributes([
    'class'=>'form-check-input',
]);
$form->get('datasetTypes')->setLabelAttributes([
    'class'=>'form-check-label',
]);

$form->get('submit')->setAttributes(['class'=>'btn btn-primary']);

$form->prepare();
?>

<?= $this->form()->openTag($form); ?>
<div class="form-group">
    <?= $this->formLabel($form->get('title')); ?>
    <?= $this->formElement($form->get('title')); ?>
    <?= $this->formElementErrors($form->get('title')); ?>
</div>
<div class="form-group">
    <?= $this->formLabel($form->get('description')); ?>
    <?= $this->formElement($form->get('description')); ?>
    <?= $this->formElementErrors($form->get('description')); ?>
</div>

<!--
***
* - Dataset type selection obsolete for now
* - All datasets are of type 'stream', which now also includes the ability to store files.
***
<div class="form-group">
    <label class="form-check-label" for="<?= $form->get('datasetTypes')->getName()?>"><?= $form->get('datasetTypes')->getLabel()?></label>
    <?php
    $firstItem = true;
    $radioArray = $form->get('datasetTypes')->getValueOptions();
    foreach ($radioArray as $key => $radioItem):
    ?>
        <div class="form-check">
            <?php
            if ($firstItem) {
                $checked = "checked";
                $firstItem = false;
            }
            else {
                $checked = "";
            }
            ?>
            <input class="form-check-input" type="radio" name="<?= $form->get('datasetTypes')->getName()?>" id="<?= $key ?>" value="<?= $key ?>" <?= $checked ?>>
            <label class="form-check-label" for="<?= $key ?>">
                <?= $radioItem ?>
            </label>
        </div>
    <?php endforeach; ?>
</div>
-->
<input type="hidden" name="datasetTypes" value="1">

<div class="form-group">
    <?= $this->formElement($form->get('submit')); ?>
</div>
<?= $this->form()->closeTag(); ?>