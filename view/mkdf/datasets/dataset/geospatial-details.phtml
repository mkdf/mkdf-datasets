<h1><?= $dataset->title ?></h1>
<div class="container">
    <div class="row">
        <div class="col-md-3">
            <?= $this->partial('mkdf/datasets/partial/dataset-features', ['features' => $features, 'dataset_id' => $dataset->id] ) ?>
        </div>
        <div class="col-md-9">
            <h2>Location information</h2>
            <?= $this->partial('mkdf/core/partial/actions', $actions); ?>
            <?php if (count($metadata) > 0): ?>
            <table class="table table-bordered">
                <?php foreach ($metadata as $metaEntry) : ?>
                    <tr>
                        <td><?= $this->escapeHtml($metaEntry['name']) ?></td>
                        <td><?= $this->escapeHtml($metaEntry['description']) ?></td>
                        <td><?= $this->escapeHtml($metaEntry['value']) ?></td>
                    </tr>
                <?php endforeach; ?>

            </table>
            <?php else: ?>
                <div class="card">
                    <div class="card-body">
                        <em>No location information currently associated with this dataset</em>
                    </div>
                </div>
            <?php endif; ?>
            <div class="card">
                <div class="card-body">
                    <div class="container-fluid m-0 p-0" id="map" style="height: 400px; width: 100%; padding:0"></div>
                </div>
            </div>

        </div>
    </div>

</div>
<?= $this->inlineScript() ?>
<?= $this->inlineScript()->prependFile($this->basePath('js/homepage-map.js')); ?>
